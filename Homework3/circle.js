module.exports.circleArea=function circleArea(radius){
let result=radius*radius*Math.PI
console.log(`${radius} yarıçaplı dairenin alanı : ${result}`)
}

module.exports.circleCircumference=function circleCircumference(radius){
let result=2*radius*Math.PI
console.log(`${radius} yarıçaplı dairenin çevresi : ${result}`)
}
